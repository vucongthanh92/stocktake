name: my_app
description: A new Flutter application.
version: 1.0.0+1
environment:
  sdk: ">=2.1.0 <3.0.0"

dependencies:
  flutter:
    sdk: flutter
  cupertino_icons: ^0.1.2
  http: ^0.12.0
  url_launcher: ^4.0.3
  shared_preferences: ^0.5.3+4
  flutter_local_notifications:
  firebase_messaging: ^2.0.0

dev_dependencies:
  flutter_test:
    sdk: flutter
    
flutter:
  uses-material-design: true
  assets:
    - assets/images/flutter-logo-round.png
    - assets/images/bg-drawer.jpeg
    - assets/images/default-user.jpg